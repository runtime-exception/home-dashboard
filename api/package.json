{
  "name": "home-dashboard-api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Tools Center 配置管理服务",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "node --watch src/server.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node test/probe-semantics.test.ts && node test/config-search.test.ts"
  },
  "dependencies": {
    "fastify": "^5.12.4",
    "yaml": "^2.9.1",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
