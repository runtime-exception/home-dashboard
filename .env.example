# 门户对外端口
DASHBOARD_PORT=8080

# ── 控制台账号 ──────────────────────────────────────────────
# 两项都必须设置，否则 api 容器不会启动。
# ADMIN_TOKEN 至少 8 个字符，建议用以下命令生成：
#   openssl rand -base64 24
ADMIN_USERNAME=admin
ADMIN_TOKEN=

# 登录会话有效期，单位小时，默认 8
SESSION_TTL_HOURS=8
